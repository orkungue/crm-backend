json.extract! @user, :id, :username, :firstname, :lastname, :eos_entity_id, :gender
# hash = {}
# @customer.class.column_names.each do |x|
#   hash[x.to_sym] = @customer.send(x)
# end
# json.merge! hash
